#include <iostream>
#include <sstream>
#include <vector>
#include <cmath>
using namespace std;
/* Function that checks if a number is prime */

int main() {
  int n;

  /* Read the value of n from the user */
  
  cin >> n;
  int m;
  m = (n/2);

  /* Print the result */
  cout << m;

  return 0;
}